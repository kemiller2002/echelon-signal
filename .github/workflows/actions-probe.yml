name: Actions Probe

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Actions runner
        run: |
          echo "GitHub Actions is executing for $GITHUB_REPOSITORY at $GITHUB_SHA"
