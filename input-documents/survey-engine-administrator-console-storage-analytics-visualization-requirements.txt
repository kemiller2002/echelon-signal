# Echelon Signal - Administrator Console, Storage, Analytics, and Visualization Requirements

Status: Authoritative requirements extension
Added: 2026-09-22
Purpose: Define the administrator-side product for importing survey submissions, aggregating results, selecting durable storage, exploring results, building reports and dashboards, and rendering accessible visualizations while preserving Echelon Signal's deterministic, no-PII architecture.

This document extends, but does not replace:

- `survey-engine-admin-result-persistence-model.txt`
- `survey-engine-reporting-requirements-data-contract.txt`
- `survey-engine-advanced-ros-ordo-limen-stress-requirements.txt`

The earlier contracts remain authoritative for SurveyResult, SurveyGroupResult, AdminReportState, reporting boundaries, anonymous behavior, deterministic interpretation, Ordo state modeling, Limen browser boundaries, unknown effects, and no-PII requirements.

---

# ADM-001 - Administrator Product Boundary and No-PII Contract

The administrator application MUST be a first-class Echelon Signal application, not a collection of ad hoc pages around storage APIs.

The administrator application MUST support:

- Survey-group creation and management
- Completed submission import
- Import validation and rejection handling
- Aggregate status and completion tracking
- Durable administrator-state persistence
- Storage-provider selection and configuration
- Report and dashboard authoring
- Interactive analysis
- Accessible visualization
- Report snapshots and exports
- Audit and lineage inspection
- Storage migration and reconciliation

The administrator application MUST NOT collect, persist, display, join, infer, or export PII.

The administrator application MUST NOT add names, email addresses, employee IDs, phone numbers, postal addresses, account profiles, or other person-level identifiers to survey state.

For identified surveys, SurveyInstanceId remains an opaque survey-instance identifier. Any mapping from SurveyInstanceId to a real person remains outside Echelon Signal.

Administrator credentials are transport secrets, not domain data. Storage-provider authentication material MUST NOT become part of SurveyGroupResult, AdminReportState, report definitions, report exports, URLs, telemetry, or audit records.

The application MUST NOT read or display GitHub user profile information merely because a GitHub token is present.

Any storage provider that exposes account identity, commit-author identity, or other external account metadata MUST keep that metadata outside Signal's domain model and report model.

Free-form administrator notes SHOULD NOT be stored inside Echelon Signal because they create a path for PII. If narrative report commentary is later supported, it MUST have a separate explicit privacy design and validation policy.

---

# ADM-002 - Administrator State System

Administrator behavior MUST be modeled as explicit legal state and transitions under Ordo.

At minimum the administrator state model MUST distinguish:

- Unconfigured
- StorageConfigured
- GroupDraft
- GroupReady
- Importing
- GroupPartial
- GroupComplete
- Reporting
- Snapshotting
- Reconciling
- StorageMigrationPlanned
- StorageMigrationRunning
- StorageMigrationVerified
- DegradedReadOnly
- InvalidConfiguration

The exact model MAY use nested state machines rather than one flat union, but illegal combinations MUST be unrepresentable or explicitly rejected.

Representative commands include:

- ConfigureStorage
- VerifyStorage
- CreateGroup
- UpdateGroupConfiguration
- ImportSubmission
- ImportSubmissionBatch
- RejectImport
- AcceptImport
- ReconcileUnknownImport
- RefreshFromStore
- GenerateReportProjection
- SaveReportDefinition
- CreateReportSnapshot
- ExportReport
- BeginStorageMigration
- VerifyStorageMigration
- ActivateMigratedStore
- RepairDerivedIndex
- RebuildAggregateProjection

Representative capabilities include:

- CanConfigureStorage
- CanCreateGroup
- CanImportSubmission
- CanImportBatch
- CanViewPartialReport
- CanFinalizeGroup
- CanBuildReport
- CanCreateSnapshot
- CanExport
- CanMigrateStorage
- CanRepairIndex
- CanWriteStore
- CanReadStore
- CanReconcile

Capabilities MUST be derived from authoritative F# state.

Limen, HTML, CSS, JavaScript/TypeScript, and storage adapters MUST NOT independently recreate capability policy.

Unresolved work MUST be represented as obligations.

Representative obligations include:

- ConfigureDurableStore
- ResolveTemplate
- ReconcileUnknownWrite
- ResolveConcurrentModification
- RepairCorruptManifest
- RebuildDerivedProjection
- ResolveUnsupportedStoreCapability
- VerifyMigration
- ResolveIncompatibleSurveyVersions
- ResolvePrivacyThresholdViolation

---

# ADM-003 - Storage Provider Abstraction

Administrator persistence MUST be defined through a storage-provider contract rather than through GitHub-specific domain logic.

The domain SHOULD reason about a capability-oriented abstraction conceptually equivalent to:

```
StorageProvider
    ProviderType
    ProviderVersion
    Capabilities
    Locator
    Read
    Write
    CompareAndSwap
    List
    Delete?
    Batch?
    Query?
    Transaction?
    Reconcile
```

Storage-provider capabilities MUST be explicit and versioned.

Representative capabilities include:

- ReadObject
- WriteObject
- ConditionalWrite
- AtomicReplace
- ListPrefix
- BatchRead
- BatchWrite
- ServerSideQuery
- ServerSideAggregate
- Transactions
- ImmutableObject
- DeleteObject
- StrongDelete
- VersionHistory
- Compression
- EncryptionAtRestClaim
- MaxObjectSize
- MaxBatchSize
- RateLimitObservation
- ChangeToken
- StreamingRead
- StreamingWrite

The application MUST NOT assume that every provider has the same capabilities.

Unsupported provider behavior MUST produce explicit negative knowledge or unavailable capability state rather than hidden fallback behavior.

Storage-provider selection MUST NOT change survey scoring, aggregation meaning, report meaning, duplicate rules, privacy rules, or result hashes.

The same logical administrator state stored through two conforming providers MUST reconstruct the same canonical SurveyGroupResult and report projections.

---

# ADM-004 - Phase 1 GitHub Repository Storage Provider

The first durable external storage provider MUST be a GitHub repository.

The GitHub provider MUST allow an administrator to configure at minimum:

- Repository owner/name
- Branch
- Root path
- Authentication token or equivalent credential mechanism
- Read-only versus read/write mode where token capabilities permit
- Optional storage namespace when multiple Signal datasets share one repository

The GitHub token MUST NOT be written to the repository, browser URL, report export, logs, telemetry, crash data, or serialized administrator state.

The application SHOULD prefer a fine-grained token scoped only to the required repository and permissions.

The application MUST verify required repository capabilities before enabling write operations.

The GitHub provider SHOULD use canonical JSON or another deterministic text format for domain artifacts so history remains inspectable and diffable.

A recommended logical storage layout is:

```
signal/
  manifest/
    storage-manifest.json
  groups/
    {group-id}/
      group-config.json
      accepted-set.json
      current-aggregate.json
      current-report-state.json
      indexes/
      snapshots/
  results/
    {prefix}/
      {result-id-or-hash}.json
  report-definitions/
    {report-definition-id}/
      {version}.json
  report-snapshots/
    {snapshot-id}.json
  audit/
    ...
```

The exact physical layout MAY evolve, but it MUST be versioned and described by a StorageManifest.

The provider SHOULD favor immutable/content-addressed result objects plus small mutable manifests over repeatedly rewriting one monolithic file.

Large collections SHOULD be partitioned by stable hash prefix or another deterministic sharding strategy.

Git history MAY provide additional auditability, but Signal MUST NOT depend on Git commit author identity as application data.

Git history MUST NOT be treated as a substitute for explicit Signal hashes, schema versions, lineage, or reconciliation records.

---

# ADM-005 - Canonical Storage Manifest

Every durable administrator store MUST contain or expose a canonical storage manifest.

The manifest SHOULD include:

- StorageSchemaVersion
- ProviderContractVersion
- DatasetId
- RootNamespace
- CanonicalizationVersion
- ResultSchemaVersion
- AdminStateSchemaVersion
- ReportDefinitionSchemaVersion
- VisualizationSchemaVersion
- SupportedEncodingVersions
- CreatedFromApplicationVersion
- ActiveMigrationState
- Integrity metadata
- Optional provider capability snapshot

The manifest MUST NOT contain PII.

The application MUST validate the manifest before loading mutable administrator state.

Unsupported future manifest versions MUST fail explicitly.

A partially completed storage migration MUST be represented in the manifest and MUST NOT be mistaken for a fully active store.

---

# ADM-006 - Future Installable Service Storage Option

A future phase MUST be able to add an installable service with HTTP endpoints as another StorageProvider implementation.

The current architecture MUST reserve this option without requiring the service in phase 1.

The domain MUST NOT hardcode GitHub concepts such as commits, branches, repositories, or file paths into SurveyGroupResult, AdminReportState, ReportData, or scoring types.

A future service provider MAY offer capabilities that GitHub does not, including:

- Transactions
- Server-side querying
- Server-side aggregation
- Background ingestion
- Scheduled report generation
- Push notifications
- Larger datasets
- Streaming import/export
- Stronger deletion semantics
- Centralized access control

The endpoint shape itself is intentionally deferred to the later service phase.

The phase-1 client MUST be able to discover that these capabilities are unavailable rather than simulate them incorrectly.

---

# ADM-007 - Group Administration

The administrator application MUST provide a complete group-management experience.

An administrator MUST be able to:

- Create a survey group
- Select the immutable survey/template definition used by the group
- Select identity mode
- Configure expected survey count where applicable
- Configure accepted template/version policy
- Configure group completion policy
- Configure report definitions
- Configure privacy/suppression thresholds
- Select storage profile
- View current group status
- Close an incomplete group where policy allows
- Reopen only when the group policy and state model explicitly allow it
- Create a successor group without mutating prior immutable results

Group configuration changes that alter interpretation MUST be versioned.

Changes that would invalidate already accepted results MUST be blocked or require creation of a new group/configuration version.

The UI MUST clearly distinguish:

- Draft configuration
- Active collection/import state
- Partial result state
- Complete result state
- Closed incomplete state
- Superseded state

---

# ADM-008 - Submission Intake and Import Queue

Phase 1 MUST support administrator-driven import of completed respondent artifacts.

At minimum support:

- Paste one completed survey URL
- Paste multiple completed survey URLs
- Import a text file containing completed survey URLs
- Import a canonical submission artifact if supported by the existing response contract
- Reprocess previously rejected imports after the blocking issue is resolved

The import pipeline MUST use the authoritative existing import semantics:

Completed artifact
-> parse
-> resolve exact immutable template
-> verify fingerprint/integrity
-> decode
-> validate
-> evaluate
-> score
-> create SurveyResult
-> enforce group acceptance and duplicate rules
-> persist accepted contribution
-> update aggregate projection

The administrator UI MUST expose separate views for:

- Pending imports
- Accepted imports
- Rejected imports
- Duplicate imports
- Imports requiring reconciliation
- Imports blocked by missing template/version
- Imports blocked by unsupported encoding/capability

Rejected imports MUST NOT affect group scores, counts, charts, or completion.

Batch import MUST be deterministic regardless of input ordering.

If two valid submissions are independent, accepting A then B MUST produce the same final group result as accepting B then A.

---

# ADM-009 - Import Idempotency, Concurrency, and Unknown Effects

Every import MUST have an idempotency identity derived from canonical submission identity and group context.

A retried import MUST NOT create a second contribution.

GitHub writes MUST use optimistic concurrency or equivalent compare-and-swap semantics.

If two administrators import different valid submissions concurrently, the system SHOULD be able to merge the resulting independent immutable contributions deterministically.

The preferred model is set-based:

```
AcceptedContributionSet
    keyed by immutable accepted submission/result identity

SurveyGroupResult
    = deterministic aggregate(AcceptedContributionSet, GroupConfiguration)
```

Concurrent additions of distinct contributions SHOULD commute.

Concurrent incompatible edits to mutable configuration MUST NOT be silently merged.

If a write may have reached GitHub but the client cannot determine the outcome, the result MUST be OutcomeUnknown rather than Failed.

OutcomeUnknown MUST create a reconciliation obligation.

Before retrying an unknown write, the application MUST inspect the provider state and determine whether the intended immutable object and/or manifest transition already occurred.

Blind retry after OutcomeUnknown is forbidden when it could duplicate or overwrite state.

---

# ADM-010 - Durable Result and Aggregate Storage Model

The durable store SHOULD separate immutable accepted results from mutable derived projections.

Recommended layers:

1. Immutable accepted SurveyResult records
2. AcceptedContributionSet / index
3. Deterministically derived SurveyGroupResult
4. Deterministically derived AdminReportState
5. Saved ReportDefinition records
6. Immutable ReportSnapshot records
7. Rebuildable indexes/caches

A derived index or cached aggregate MUST be rebuildable from authoritative immutable inputs.

Derived projections MUST have hashes identifying the source set/configuration from which they were calculated.

The system MUST be able to detect when:

- A current aggregate is stale
- A current report projection is stale
- An index is stale
- A snapshot references a missing or incompatible source
- The store contains an immutable result not yet reflected in the current aggregate manifest

Repair MUST be an explicit legal transition, not an invisible UI side effect.

---

# ADM-011 - Incremental Aggregation Engine

The administrator application MUST support incremental aggregation.

Importing one new accepted result SHOULD recompute only affected aggregate nodes rather than scanning all historical results when the existing aggregate state contains sufficient evidence.

Incremental aggregation MUST remain observationally equivalent to full deterministic recomputation from the accepted contribution set.

The following invariant applies:

```
IncrementalGroupAggregate(previousAggregate, newContribution)
=
FullGroupAggregate(allAcceptedContributions)
```

for every legal accepted-contribution sequence.

The aggregate execution plan SHOULD reuse the same dependency-graph principles used by the survey evaluator.

Aggregate state SHOULD distinguish at minimum:

- Count
- Sum
- Mean support values
- Min/max
- Category counts
- Distribution bins when configured
- Missing/NA/Don't-Know counts
- Coverage
- Confidence inputs
- Recommendation frequencies
- Section/domain aggregates
- Comparison-ready metric values

If a statistic cannot be safely updated incrementally, the engine MAY mark the affected aggregate node as requiring rebuild rather than calculate an approximation.

---

# ADM-012 - Query and Analysis Model

The administrator application MUST provide a deterministic analysis layer over SurveyGroupResult and approved stored result detail.

The analysis layer SHOULD support:

Dimensions:
- SurveyIdentifier
- TemplateVersion where comparison is legal
- SurveyGroupId
- Section/domain
- Question/metric identifier where report policy allows
- Result category
- Recommendation identifier
- Group-level time period or wave identifier
- Other non-PII, explicitly declared dimensions

Measures:
- Accepted response count
- Completion rate
- Coverage
- Mean
- Median
- Minimum
- Maximum
- Standard deviation
- Percentiles
- Category frequency
- Percentage/proportion
- Don't-Know rate
- Not-Applicable rate
- Missing rate
- Recommendation frequency
- Score distribution
- Change from prior compatible group/snapshot

The analysis system MUST distinguish raw canonical result values from calculated analysis metrics.

Calculated analysis metrics MUST NOT mutate or replace canonical SurveyResult or SurveyGroupResult values.

A typed, pure, serializable AnalysisExpression AST MAY be used for calculated measures.

The AnalysisExpression language MUST be versioned, deterministic, resource-bounded, and free of I/O, clock access, randomness, reflection, or arbitrary code execution.

---

# ADM-013 - Statistical and Measurement Analysis

The administrator analysis layer SHOULD support advanced statistical summaries when the underlying metric semantics and sample size make them meaningful.

Potential capabilities include:

- Confidence intervals
- Interquartile range
- Median absolute deviation
- Effect size between compatible groups
- Change over repeated waves
- Correlation matrix for approved numeric metrics
- Item-total correlation
- Response entropy
- Ceiling/floor effect detection
- Internal-consistency estimates such as Cronbach's alpha when mathematically appropriate
- Distribution skew indicators
- Outlier flags on aggregate/non-identifying values

Advanced statistics MUST NOT be displayed merely because they can be calculated.

Each statistic MUST define prerequisites such as:

- Minimum sample size
- Metric scale assumptions
- Missing-value policy
- Comparable template semantics
- Privacy threshold
- Required data detail

If prerequisites are not met, the result MUST be a typed unavailable/insufficient state.

The UI MUST distinguish descriptive statistics from inferential statistics.

Confidence intervals and similar uncertainty displays MUST identify the method used.

No statistical output may imply causal inference unless the survey design explicitly supports it.

---

# ADM-014 - Cross-Group, Historical, and Version Comparison

Administrators SHOULD be able to compare compatible groups, waves, or snapshots.

Comparison MUST be based on explicit semantic compatibility, not merely matching labels.

A comparison candidate SHOULD consider:

- SurveyIdentifier
- Metric/section/question stable identifier
- TemplateVersion
- SemanticHash
- Scoring algorithm/version
- Scoring-expression language version
- Result direction
- Scale/range
- Applicability semantics
- Aggregation semantics
- Privacy policy

The engine MUST return NotComparable when semantics differ materially.

A report MAY show the reason comparison was rejected.

If a later template explicitly declares a metric as backward-compatible with an earlier metric, that compatibility declaration MUST be versioned and validated.

Historical comparison MUST never retroactively mutate the original result.

Re-evaluating old submissions under new scoring rules, if later supported, MUST create a separately labeled derived analysis result.

---

# ADM-015 - Signal Visualization Grammar

Echelon Signal SHOULD implement a typed declarative visualization system rather than allowing UI components to invent chart semantics independently.

Conceptual model:

```
VisualizationSpec
    VisualizationSpecVersion
    Intent
    DataBinding
    Encodings
    Scale
    Sort
    AggregationReference
    Labels
    Accessibility
    Interaction
    PrivacyClass
```

Visualization semantics MUST be produced or validated by F#.

Limen/browser code MAY render the resulting visualization specification using SVG, Canvas, HTML, or another mechanism, but MUST NOT invent new business calculations.

Initial chart types SHOULD include:

- Bar
- Grouped bar
- Stacked bar
- 100% stacked bar
- Line
- Area
- Dot plot
- Distribution/histogram
- Box plot
- Heatmap
- Radar/profile chart where semantically appropriate
- Progress/bullet chart
- Small multiples
- Ranked table
- Metric cards
- Matrix
- Sparklines for compact historical views

Pie/donut charts MAY be supported only for small categorical sets with explicit suitability rules.

3D charts SHOULD NOT be supported.

Dual-axis charts SHOULD be disabled by default and require an explicit report-definition justification because they are easy to misread.

Every chart type MUST declare legal dimension/measure shapes.

Invalid chart/data combinations MUST fail validation rather than render misleading output.

---

# ADM-016 - Visualization Suitability and Anti-Misleading Rules

The visualization compiler SHOULD validate chart suitability.

Representative rules include:

- A line chart requires an ordered dimension.
- A histogram requires a numeric distribution.
- A 100% stacked chart requires a defined denominator and explicit missing-value policy.
- Category charts with excessive cardinality should recommend grouping/table presentation.
- Truncated quantitative axes MUST be explicit and SHOULD generate a warning for bar charts.
- Percent axes MUST define whether values are 0-1 or 0-100.
- Sorting MUST be deterministic.
- Missing values MUST not silently become zero.
- Suppressed privacy cells MUST not be inferable from totals when suppression policy forbids it.
- Color MUST NOT be the only carrier of meaning.
- Legends MUST map to stable semantic categories.
- The same category SHOULD have stable visual encoding across one report when practical.

The compiler MAY emit warnings, suggestions, and alternate chart candidates.

Automated visualization recommendations MUST remain advisory. The authoritative output is the validated VisualizationSpec chosen by the report definition.

---

# ADM-017 - Visualization Accessibility

Every visualization MUST have an accessible equivalent.

At minimum:

- Text alternative/title
- Programmatic description
- Keyboard-accessible focus where interaction exists
- Tabular equivalent for charted data
- Screen-reader meaningful labels
- Sufficient contrast
- Non-color encoding such as labels, patterns, line style, symbols, or position
- Zoom/reflow support
- No required hover-only interaction
- Touch targets suitable for mobile/tablet use
- Motion reduction support where animation exists

Default palettes MUST account for common color-vision deficiencies.

The system MUST support high-contrast presentation.

Visualizations MUST remain understandable in grayscale/print where the report definition allows printing.

Typography and spacing SHOULD support users with low vision and dyslexia without requiring a special font.

Chart labels and summaries SHOULD use plain language and avoid forcing the user to decode a legend when direct labeling is practical.

An accessibility validation pass SHOULD reject or warn on:

- Color-only distinctions
- Insufficient contrast
- Unlabeled interactive marks
- Data only available on hover
- Tiny fixed text
- Inaccessible canvas-only content without equivalent semantics
- Keyboard traps
- Excessive animation

---

# ADM-018 - Dashboard System

The administrator application SHOULD support saved dashboards built from deterministic report/visualization components.

A dashboard definition SHOULD be versioned and declarative.

Representative dashboard blocks:

- Group status
- Import status
- Overall score/result
- Section/domain summary
- Distribution
- Trend
- Comparison
- Recommendation frequency
- Coverage/confidence
- Missing/Don't-Know analysis
- Privacy/suppression warnings
- Recent snapshot status
- Storage health
- Reconciliation obligations

Administrators SHOULD be able to:

- Add/remove blocks
- Reorder blocks
- Resize within supported layout constraints
- Change approved visualization type
- Select compatible metrics
- Apply non-PII filters
- Save a dashboard definition
- Duplicate a dashboard definition
- Version a dashboard definition
- Reset to a known report template

Dashboard layout MUST be responsive.

The same dashboard definition SHOULD support desktop, tablet, and mobile projections without storing separate business meaning.

---

# ADM-019 - Interactive Exploration and Drill-Down

The administrator application SHOULD support interactive analysis without requiring report-definition changes for every exploration.

Supported interactions MAY include:

- Filter
- Sort
- Search by opaque identifier
- Expand/collapse
- Select a chart mark
- Drill from overall result to section/domain
- Drill from section/domain to aggregate question metrics where policy permits
- Compare compatible groups
- Toggle count/percentage
- Change time window
- View distribution detail
- View calculation explanation
- View lineage

Interactive exploration MUST NOT alter canonical stored results.

Every derived view MUST be reproducible from an explicit AnalysisState.

AnalysisState MAY be stored in the administrator URL when it fits, allowing bookmark/share behavior without embedding secrets.

Credentials MUST never be encoded into AnalysisState or URLs.

Back/Forward browser navigation SHOULD restore analysis state through Limen history integration.

---

# ADM-020 - Data-Lineage Explorer

The administrator application SHOULD provide a lineage/explanation view capable of tracing a reported value backward.

For a selected chart point, metric card, recommendation frequency, or table value, the system SHOULD be able to identify:

- ReportDefinitionVersion
- VisualizationSpecVersion
- AnalysisExpressionVersion if used
- Aggregate node
- SurveyGroupResultHash
- Accepted contribution/result hashes
- Template/semantic hashes
- Scoring versions
- Relevant reason codes
- Snapshot identity where applicable

The lineage view MUST respect privacy policy.

For anonymous groups it MUST NOT expose person-level linkage or reconstruct invitation identity.

Lineage MUST be derived from the same authoritative evaluation/aggregation path that produced the displayed value.

---

# ADM-021 - Report Builder

Administrators SHOULD be able to create report definitions through a structured report builder.

The builder SHOULD support:

- Audience selection
- Report block selection
- Block ordering
- Visualization selection
- Metric binding
- Filter rules
- Comparison rules
- Privacy rules
- Detail level
- Rounding rules
- Localization
- Print behavior
- Export formats
- Accessibility checks
- Preview using current group state
- Validation before publication/use

Report definitions MUST be versioned.

A report definition used for a formal snapshot MUST become immutable at that version.

Editing a used report definition MUST create a new version.

The builder MUST NOT allow presentation rules to override scoring, applicability, duplicate handling, completion, or canonical aggregation.

---

# ADM-022 - Report Snapshots and Reproducibility

A formal report snapshot MUST be an immutable record of a report generated from an exact result state and report definition.

Recommended fields:

```
ReportSnapshot
    ReportSnapshotId
    SurveyGroupId
    SurveyGroupResultHash
    AdminReportStateHash
    ReportDefinitionId
    ReportDefinitionVersion
    VisualizationSchemaVersion
    ComparisonReferences
    Locale
    CanonicalReportDataHash
    GeneratedAtEvidence?
    SnapshotSchemaVersion
```

GeneratedAt MUST NOT affect canonical report meaning.

If time is recorded, it MUST come from explicit clock evidence and be separated from deterministic content hashes unless the snapshot contract intentionally includes it.

Opening a historical snapshot MUST NOT silently substitute current group state.

A snapshot whose dependencies are unavailable MUST show an explicit unresolved state.

---

# ADM-023 - Exports

The administrator system SHOULD support:

- Structured JSON
- CSV for legal tabular projections
- Print-friendly HTML
- PDF as a renderer/output format
- Static snapshot package for offline review where feasible

Exports MUST be derived from the same ReportData used by on-screen rendering.

CSV MUST NOT be used as the canonical representation for composite results.

Every export SHOULD include sufficient non-PII lineage metadata to identify:

- Survey/group
- Result schema version
- Report definition version
- Snapshot/result hash
- Comparison references where used

Exports MUST enforce the same privacy suppression rules as the screen.

Exporting MUST NOT reveal data hidden by the current report policy.

---

# ADM-024 - Privacy-Preserving Aggregation and Disclosure Controls

No-PII does not eliminate inference risk.

The administrator system MUST support disclosure controls for small groups and small slices.

At minimum support:

- Minimum group size
- Minimum subgroup/cell size
- Suppression of small anonymous slices
- Suppression-aware totals when subtraction could reveal a hidden cell
- Minimum distribution sample size
- Minimum comparison sample size
- Optional minimum result diversity before showing a distribution

Privacy policy MUST be evaluated before ReportData is emitted to visual components.

The UI MUST NOT receive hidden row-level data and then merely hide it with CSS.

A filtered view that reduces an anonymous group below the configured threshold MUST become suppressed.

Repeated interactive filtering MUST NOT provide an easy differencing attack that reconstructs suppressed values.

The system SHOULD detect and block obvious differencing paths when two displayed aggregates can reveal a suppressed small group.

---

# ADM-025 - Storage Security and Integrity

Storage integrity MUST be independent of transport success.

Every canonical stored domain object SHOULD have a deterministic content hash.

Important mutable manifests SHOULD reference immutable child object hashes.

The application MUST detect tampering, truncation, wrong-schema content, and wrong-dataset references.

GitHub transport security alone MUST NOT be treated as proof that loaded data belongs to the intended Signal dataset.

Sensitive storage locators and credentials MUST not be included in public/shareable report artifacts.

Optional client-side encryption for durable store objects MAY be introduced later, but if implemented:

- Encryption MUST be an explicit storage capability/policy.
- Key material MUST never be stored alongside ciphertext in the same provider.
- Encryption MUST not change canonical domain hashes unless the contract deliberately hashes ciphertext separately.
- Lost keys MUST produce an explicit unrecoverable state.
- Browser crypto operations MUST be mechanisms requested by authoritative F# state, not ad hoc TypeScript policy.

---

# ADM-026 - GitHub Rate Limits, Scale, and Performance

The GitHub provider MUST treat rate limits and repository limits as first-class external constraints.

The provider SHOULD observe and expose:

- Remaining request budget where available
- Retry-after/rate-limit reset evidence
- Object size failures
- Repository write conflicts
- Network interruption
- Partial list results
- Stale ETag/change token

The application MUST NOT spin or aggressively retry rate-limited operations.

Immutable objects SHOULD be cached safely because their hashes guarantee identity.

Conditional reads SHOULD be used where supported.

Large group loading SHOULD avoid fetching every immutable result when a verified aggregate and index are sufficient for the requested view.

The architecture SHOULD support at least three scale modes:

- Small: all relevant admin state can be loaded eagerly.
- Medium: indexes and aggregates are eager, immutable result detail is lazy.
- Large: partitioned indexes/aggregates, lazy detail, bounded memory, provider-aware paging.

Scale thresholds SHOULD be evidence-driven rather than hardcoded assumptions.

Performance acceptance tests SHOULD include groups large enough to force partitioning and lazy loading.

---

# ADM-027 - Rebuildable Indexes and Materialized Views

To make GitHub practical as a store, the system MAY maintain derived indexes/materialized views.

Examples:

- Accepted contribution index
- Result-by-group index
- Metric summary index
- Report-ready aggregate
- Recommendation-frequency index
- Comparison index
- Snapshot index

Every derived index MUST identify the authoritative source hash/set it represents.

Indexes MUST be disposable and rebuildable.

A corrupt or stale index MUST NOT corrupt canonical results.

The application SHOULD provide:

- ValidateIndex
- RebuildIndex
- CompareIndexToReference
- ActivateRebuiltIndex

Rebuilding an index MUST produce the same canonical index output given the same immutable source set and index version.

---

# ADM-028 - Storage Migration and Provider Portability

Administrators MUST be able to move a Signal dataset from one storage provider to another without changing report meaning.

A migration SHOULD follow:

1. Validate source store.
2. Capture immutable source manifest/hash set.
3. Create destination manifest in migration state.
4. Copy immutable objects.
5. Copy/version report definitions and snapshots.
6. Rebuild or copy verified derived indexes.
7. Verify object counts and hashes.
8. Recompute representative aggregates.
9. Compare source/destination canonical results.
10. Mark destination verified.
11. Explicitly activate destination.
12. Leave source untouched unless a later explicit retirement action occurs.

Migration MUST be resumable and idempotent.

A partially copied destination MUST NOT be presented as authoritative.

Migration verification MUST include semantic equivalence, not only byte counts.

The same mechanism SHOULD later support GitHub -> installable service migration.

---

# ADM-029 - Backup, Restore, and Disaster Recovery

A storage provider SHOULD support export of a provider-independent Signal backup bundle.

The bundle SHOULD contain:

- Storage manifest
- Group configurations
- Immutable accepted result objects
- Accepted contribution sets
- Report definitions
- Report snapshots
- Required non-PII comparison metadata
- Hash manifest

Derived caches MAY be excluded when they are rebuildable.

Restore MUST verify hashes and schema versions before activation.

A restore MUST never merge two datasets merely because their group IDs collide.

DatasetId and content lineage MUST participate in restore validation.

---

# ADM-030 - Audit Without PII

Signal audit records MUST describe what the system did without recording person identity or answer content unnecessarily.

Representative audit events:

- StorageConfigured
- StorageVerified
- GroupCreated
- GroupConfigurationVersioned
- SubmissionAccepted
- SubmissionRejected
- DuplicateRejected
- UnknownWriteObserved
- WriteReconciled
- AggregateRebuilt
- IndexRebuilt
- ReportDefinitionPublished
- ReportSnapshotCreated
- ExportCreated
- MigrationStarted
- MigrationVerified
- DestinationActivated

Audit records SHOULD contain:

- Event type
- Opaque domain identifiers
- Content hashes
- Schema versions
- Reason codes
- Before/after state identifiers
- Explicit clock evidence when required

Audit records MUST NOT contain:

- Names
- Emails
- GitHub login names
- Free-form user commentary
- Authentication tokens
- Raw respondent URLs unless a separately configured retention policy explicitly requires a canonical submission artifact and the audit event references it by hash instead

---

# ADM-031 - Administrator UX Information Architecture

The administrator UI SHOULD expose clear top-level areas equivalent to:

- Overview
- Groups
- Imports
- Analysis
- Reports
- Dashboards
- Snapshots/Exports
- Storage
- Audit/Lineage
- Settings

Overview SHOULD prioritize unresolved obligations and current state, including:

- Groups waiting for imports
- Groups complete
- Rejected imports
- Unknown-effect reconciliations
- Storage health
- Stale aggregate/index warnings
- Privacy suppression warnings
- Recent snapshots
- Migration state

The UI MUST distinguish domain errors from infrastructure errors.

The UI MUST not show raw exception text as the primary administrator message.

Stable reason codes SHOULD be available in an expandable technical detail view.

---

# ADM-032 - Search, Filtering, and Saved Analysis

Administrators SHOULD be able to search and filter by allowed non-PII domain fields.

Examples:

- SurveyIdentifier
- SurveyGroupId
- TemplateVersion
- Result category
- Section/domain
- Recommendation
- Group status
- Snapshot
- Date/wave metadata where non-identifying

Search MUST NOT imply a hidden person directory.

The system MAY support saved analysis definitions consisting of:

- Filter expression
- Metric set
- Comparison references
- Sort
- Visualization selections
- Layout

Saved analysis definitions MUST be declarative, versioned, and free of credentials.

---

# ADM-033 - Read-Only and Degraded Modes

The application MUST support a useful read-only mode when the storage provider allows reads but not writes.

Read-only mode SHOULD allow:

- Browse groups
- View reports
- Run local deterministic analysis over loaded authorized data
- View snapshots
- Export if policy allows
- Inspect lineage
- Validate store health

It MUST disable write capabilities through authoritative state.

If a provider becomes temporarily unavailable after state is loaded, the application MAY enter a degraded read-only mode.

Degraded mode MUST make staleness explicit.

The application MUST NOT imply that locally viewed state is current when refresh failed.

---

# ADM-034 - Schema Evolution and Compatibility

Administrator schemas MUST be independently versioned where semantics differ.

At minimum consider:

- StorageSchemaVersion
- AdminStateSchemaVersion
- AggregateSchemaVersion
- AnalysisExpressionLanguageVersion
- VisualizationSpecVersion
- DashboardDefinitionVersion
- ReportDefinitionVersion
- ReportSnapshotSchemaVersion

Loading older supported versions MUST use explicit migration/adapter logic.

Unsupported future versions MUST fail safely.

A schema migration MUST never silently change canonical survey scores.

If an aggregate schema changes, old immutable SurveyResults SHOULD remain valid inputs to a deterministic aggregate rebuild where semantic compatibility exists.

---

# ADM-035 - Limen Boundary for Administrator UI

The administrator application MUST use Limen as the browser authority boundary.

F# owns:

- State
- Commands
- Capabilities
- Obligations
- Storage-operation intent
- Import decisions
- Aggregation
- Analysis semantics
- Privacy suppression
- Report composition semantics
- Visualization specification
- Lineage
- Export eligibility

Browser/TypeScript code MAY own mechanism such as:

- DOM event capture
- File chooser
- Clipboard access
- Download initiation
- History/navigation
- SVG/Canvas drawing mechanics
- Browser cryptography mechanism if requested
- Network transport for provider calls
- Resize/viewport observation

TypeScript MUST NOT own:

- Aggregate formulas
- Privacy decisions
- Comparison eligibility
- Chart metric calculations
- Report block eligibility
- Duplicate decisions
- Storage retry policy
- Unknown-effect reconciliation policy

The administrator application SHOULD be a demanding Limen example and exercise complex two-way interaction, file input, history, responsive layout, SVG/Canvas rendering, keyboard interaction, downloads, and provider I/O.

---

# ADM-036 - Advanced Stress and Adversarial Test Program

The administrator section MUST include tests that deliberately stress the architecture.

Required scenarios include:

## Scenario A - Concurrent Imports

Two administrator sessions import different submissions into the same GitHub-backed group from the same starting revision.

Verify:

- both immutable contributions can survive,
- manifest conflict is detected,
- deterministic reconciliation produces the same final set/aggregate,
- no duplicate contribution is created.

## Scenario B - Unknown GitHub Write Outcome

Dispatch a write, lose the response, and make the client uncertain whether GitHub accepted it.

Verify:

- state becomes OutcomeUnknown,
- blind retry is unavailable,
- reconciliation inspects remote state,
- exactly one logical contribution exists after resolution.

## Scenario C - Large Dataset

Populate enough accepted results to require partitioned indexes and lazy detail loading.

Verify:

- report overview does not require reading every result object,
- full deterministic rebuild still matches the incremental aggregate,
- memory remains bounded,
- GitHub request behavior respects rate limits.

## Scenario D - Corrupt Derived Aggregate

Alter or remove the current aggregate file while leaving immutable results intact.

Verify:

- hash/lineage validation detects the problem,
- reports are not silently rendered from corrupt state,
- rebuild obligation appears,
- rebuilt aggregate matches reference recomputation.

## Scenario E - Visualization Accessibility

For every supported chart type verify:

- screen-reader text/table equivalent,
- keyboard interaction,
- high contrast,
- color-blind safe distinction,
- no hover-only information,
- responsive zoom/reflow,
- print/grayscale behavior where supported.

## Scenario F - Privacy Differencing

Create anonymous aggregate data where a small subgroup could be inferred by subtracting visible totals.

Verify suppression policy prevents reconstruction.

## Scenario G - Version Incompatibility

Compare groups with similar labels but incompatible scoring semantics.

Verify NotComparable with stable reason codes.

## Scenario H - Provider Migration

Migrate a non-trivial GitHub-backed dataset into a test implementation of the generic provider contract.

Verify:

- object hashes,
- group aggregate hashes,
- report-data hashes,
- report snapshots,
- privacy behavior,
- visualization inputs

are semantically equivalent before activation.

## Scenario I - Stale Multi-Tab Admin State

Open the same group in two tabs, mutate one, then attempt a conflicting operation from the stale tab.

Verify stale effect/version checks prevent accidental overwrite.

## Scenario J - Report Snapshot Reproduction

Create a formal report snapshot, then change current group state and report definition.

Verify the old snapshot still reconstructs the original report data exactly.

---

# ADM-037 - Property and Model-Based Tests

Property-based tests SHOULD cover:

- Import order independence for independent valid submissions
- Duplicate idempotency
- Full aggregate = incremental aggregate
- Rebuild aggregate = stored verified aggregate
- Storage round trip preserves canonical state
- GitHub provider = provider-contract reference behavior
- Migration source = destination canonical semantics
- Filter order does not change final selected set
- Report renderer does not change metric values
- Visualization transformations do not change canonical measures
- Suppressed data never reaches unauthorized visual projections
- Snapshot reproduction is stable
- Schema adapters preserve supported old semantics
- Same immutable accepted set + same configuration = same SurveyGroupResultHash
- Same ReportData + same ReportDefinition + same locale = same canonical rendered-data model

Model-based tests SHOULD generate legal and illegal administrator transition sequences and verify capability/obligation correctness.

---

# ADM-038 - Performance and Resource Limits

Administrator operations MUST define explicit resource limits.

At minimum consider:

- Maximum batch-import count per action
- Maximum pasted URL size
- Maximum import artifact size
- Maximum chart point count before aggregation/binning is required
- Maximum dashboard block count
- Maximum filter-expression complexity
- Maximum AnalysisExpression depth/node count
- Maximum loaded result-detail count
- Maximum in-memory dataset size
- Maximum GitHub object size used by Signal
- Maximum retry count
- Maximum reconciliation scan scope

Exceeding a resource limit MUST produce an explicit typed result.

The system MUST NOT freeze the browser or enter unbounded retries on hostile but bounded input.

Where a report would require too many visual marks, the system SHOULD aggregate, paginate, virtualize, or require a narrower view rather than render an unusable chart.

---

# ADM-039 - Optional Advanced Analytics Extensions

The architecture SHOULD leave room for future non-authoritative analytic extensions such as:

- Clustering of aggregate profiles
- Anomaly detection
- Survey-item quality analysis
- Benchmarking
- Forecast/trend projection
- Scenario modeling
- Recommendation impact tracking
- Natural-language report explanation

These extensions MUST NOT mutate canonical survey results.

Any probabilistic or AI-generated analysis MUST be labeled as derived analysis, include its model/method version where reproducibility matters, and remain outside deterministic scoring authority.

No AI feature may receive PII because Signal contains none.

AI-generated narrative MUST NOT be treated as audit evidence unless its source data and model invocation are separately captured under an explicit future contract.

These are future capabilities, not phase-1 implementation requirements.

---

# ADM-040 - Phase Boundaries

Phase 1 MUST focus on a fully capable browser administrator application using:

- F# authoritative domain logic
- Limen browser boundary
- GitHub repository durable storage provider
- Deterministic import and aggregation
- Interactive analysis
- Typed visualization system
- Report/dashboard definitions
- Report snapshots/exports
- Accessibility and privacy enforcement
- Reconciliation and audit

Phase 1 MUST NOT require:

- A custom Signal server
- A database server
- Background workers
- Scheduled jobs
- Webhooks
- Server-side query endpoints
- Organization/user directory
- PII identity mapping

The architecture MUST leave explicit capability slots for those future provider/service features.

---


# ADM-041 - Change-Impact Preview and Obligation Planning

Before applying any administrator change that can affect existing or future derived state, Echelon Signal SHOULD calculate and present an impact preview.

Impact-aware changes include at minimum:

- Group configuration changes
- Report definition changes
- Dashboard definition changes
- Visualization definition changes
- Privacy-threshold changes
- Comparison-policy changes
- Storage-schema changes
- Aggregate-schema changes
- Analysis-expression changes
- Storage-provider changes
- Template/version adoption for successor groups

The impact preview SHOULD classify affected artifacts as:

- Unaffected
- RequiresReprojection
- RequiresAggregateRebuild
- RequiresIndexRebuild
- RequiresRevalidation
- BecomesNotComparable
- BecomesSuppressed
- BecomesStale
- RequiresMigration
- Blocked
- UnknownImpact

The preview MUST identify why each impact exists using stable reason codes and dependency evidence.

Applying a change SHOULD create explicit obligations for any required follow-up work.

A change MUST NOT silently invalidate a previously valid snapshot, report, comparison, aggregate, or index.

Historical immutable artifacts MUST remain historically valid even when current configuration changes.

The impact preview itself SHOULD be deterministic from:

Current authoritative state
+
Proposed change
+
Versioned dependency rules

---

# ADM-042 - Administrator Sandbox and Simulation Mode

The administrator application SHOULD provide a sandbox/simulation mode for testing configuration before affecting live administrator state.

Sandbox mode SHOULD support:

- Loading a survey/template definition
- Generating or importing synthetic submissions
- Exercising import validation
- Exercising scoring and aggregation
- Exercising duplicate detection
- Exercising report definitions
- Exercising dashboards
- Exercising visualization specifications
- Exercising privacy suppression
- Exercising storage-provider behavior
- Exercising schema migrations
- Exercising comparison rules
- Exercising report snapshots
- Exercising accessibility validation

Sandbox state MUST be isolated from production administrator state.

A sandbox artifact MUST NOT become an accepted live contribution through an accidental transition.

Promotion from sandbox to live configuration, where allowed, MUST be an explicit transition that promotes configuration only, not synthetic responses or synthetic accepted results.

Sandbox artifacts SHOULD be visibly marked as synthetic/test data.

Sandbox URLs, exports, snapshots, and reports MUST clearly indicate that they are non-production artifacts.

---

# ADM-043 - Synthetic and Adversarial Survey Data Generator

Echelon Signal SHOULD include a deterministic synthetic-data generator for administrator testing and preview.

The generator SHOULD be able to create:

- Fully valid submissions
- Partially complete submissions
- Invalid submissions
- Boundary-value submissions
- Highly skewed response distributions
- Uniform distributions
- Sparse responses
- High Don't-Know usage
- High Not-Applicable usage
- Highly correlated items
- Weakly correlated items
- Contradictory patterns where the survey permits them
- Privacy-threshold edge cases
- Duplicate submissions
- Large-volume result sets
- Version-mismatch cases
- Corrupt/tampered artifacts
- Pathological but bounded expression/analysis inputs

Synthetic generation MUST respect the no-PII constraint.

Generation SHOULD be seedable so a scenario can be reproduced exactly.

A generated dataset SHOULD record:

- GeneratorVersion
- Seed
- Template/version identity
- Generation profile
- Intended edge cases
- Expected high-level properties where defined

Synthetic data SHOULD be usable for:

- Administrator preview
- Report-design preview
- Visualization testing
- Accessibility testing
- Performance testing
- Privacy testing
- Storage testing
- Provider conformance testing

---

# ADM-044 - Storage Provider Conformance Suite

Every StorageProvider implementation MUST be testable against a shared provider-conformance suite.

The suite SHOULD verify at minimum:

- Canonical round-trip behavior
- Immutable object reads/writes
- Conditional writes
- Compare-and-swap behavior
- Concurrent modification handling
- Idempotent repeated writes
- OutcomeUnknown reconciliation
- Missing-object semantics
- Corrupt-object behavior
- Unsupported-capability behavior
- Partial listing behavior
- Pagination
- Rate-limit behavior
- Oversized object behavior
- Authentication failure behavior
- Read-only mode
- Network interruption
- Stale change tokens/ETags
- Schema-version incompatibility
- Migration source behavior
- Migration destination behavior
- Provider capability discovery

A provider MUST NOT claim a capability unless the conformance suite can verify its contract or the capability is explicitly marked unverifiable with evidence.

The GitHub provider is the initial reference provider.

A future installable Signal service MUST pass the same provider-contract suite plus tests for any additional capabilities it advertises.

Provider-specific optimizations are permitted only when they preserve the shared semantic contract.

---

# ADM-045 - Data Lifecycle, Retention, Archival, and Deletion Semantics

Echelon Signal MUST model administrator data lifecycle explicitly.

At minimum consider states or policies equivalent to:

- Active
- Archived
- RetainedForAudit
- Superseded
- Retired
- DeletedFromActiveState
- ProviderDeletionRequested
- ProviderDeletionVerified
- ProviderDeletionNotProvable

Retention policy SHOULD be configurable by artifact class, including:

- Accepted SurveyResult
- SurveyGroupResult
- AdminReportState
- Derived indexes
- Report definitions
- Report snapshots
- Comparison data
- Audit records
- Synthetic/test artifacts
- Backup bundles

Deletion semantics MUST reflect actual provider behavior.

For GitHub, deleting a current file does not necessarily erase prior Git history.

Therefore Signal MUST distinguish:

- Removal from current application state
- Removal from current repository tree
- Provider-level historical retention
- Verified irreversible deletion where a provider can prove it

The UI MUST NOT claim that data is permanently erased when the provider cannot establish that fact.

Lifecycle transitions MUST be auditable without storing PII.

Archival MUST preserve enough lineage to reproduce formal snapshots that are intentionally retained.

A retired dataset MUST become read-only by default.

---

# ADM-046 - Durable Store as Untrusted Input

All content loaded from a storage provider MUST be treated as untrusted input even when it was previously written by Signal.

The loader MUST validate:

- Path/locator validity
- Object size
- Schema version
- Canonical encoding
- Content hash
- Dataset identity
- Object type
- Required references
- Reference cycles
- Expression complexity
- Visualization complexity
- Dashboard complexity
- Analysis-expression limits
- Manifest consistency
- Cross-object version compatibility

A human may manually edit GitHub repository files.

A compromised credential may alter repository state.

A provider may return truncated, stale, duplicated, or malformed content.

Therefore repository provenance alone is not proof of validity.

Malformed or hostile durable objects MUST fail with typed reason codes.

A malformed object MUST NOT cause:

- Arbitrary code execution
- Unbounded allocation
- Unbounded recursion
- Infinite retry
- Silent data loss
- Partial domain activation

Loading a durable store SHOULD be staged so validation completes before mutable administrator capabilities become available.

---

# ADM-047 - Portable Configuration Packages

Echelon Signal SHOULD support provider-independent configuration packages that can move administration design without moving response data.

A configuration package MAY include:

- Group configuration templates
- Report definitions
- Dashboard definitions
- Visualization specifications
- Privacy policies
- Comparison policies
- Analysis expressions
- Localization resources
- Accessibility policy
- Export policy
- Storage-independent schema/configuration metadata

A configuration package MUST NOT include:

- Authentication credentials
- GitHub tokens
- Provider secrets
- Accepted respondent results unless the package is explicitly a data backup rather than a configuration package
- PII

Configuration packages MUST be versioned and integrity protected.

Import MUST validate every contained definition before activation.

Configuration-package import SHOULD support dry-run impact preview.

Imported configuration provenance SHOULD record:

- Package identity/hash
- Package schema version
- Source version where declared
- Import time evidence where needed
- Any applied compatibility adapters

Configuration activation MUST be explicit.

---

# ADM-048 - Versioned Policy Packs

Echelon Signal SHOULD support versioned policy packs for reusable administrator constraints.

A policy pack MAY define rules for:

- Privacy thresholds
- Anonymous suppression
- Differencing protection
- Accessibility requirements
- Allowed visualization types
- Export restrictions
- Report-detail defaults
- Comparison prerequisites
- Retention defaults
- Snapshot requirements
- Audit requirements
- Storage requirements
- Minimum validation/test expectations

Examples could include concepts such as:

- StrictAnonymous
- ExecutiveReporting
- ResearchAudit
- AccessibilityStrict
- OrganizationDefined

Policy packs MUST expand into explicit deterministic constraints.

They MUST NOT contain arbitrary executable code.

A policy pack MUST have:

- PolicyPackId
- Version
- Declared rules
- Compatibility requirements
- Optional parent/base policy
- Deterministic resolution semantics

Conflicting inherited policy rules MUST fail explicitly.

Formal report snapshots SHOULD record the effective policy-pack identities/versions that constrained them when relevant.

---

# ADM-049 - Template Upgrade and Successor-Group Impact Analysis

When an administrator chooses a newer survey/template version for a successor group, Signal SHOULD explain the consequences before activation.

The upgrade impact analysis SHOULD identify:

- Added metrics
- Removed metrics
- Changed metric semantics
- Changed scoring
- Changed scoring-expression language
- Changed applicability
- Changed completion rules
- Changed category thresholds
- Changed recommendation logic
- Changed aggregation semantics
- Changed privacy implications
- Comparison compatibility
- Dashboard impacts
- Report-definition impacts
- Visualization impacts
- Required index/aggregate schema changes

The system SHOULD answer questions equivalent to:

- Which existing reports still work unchanged?
- Which comparisons remain valid?
- Which charts lose a required metric?
- Which metrics have semantic continuity?
- Which saved analyses become invalid?
- Which policy packs no longer validate?
- Which historical groups remain directly comparable?

A template upgrade MUST NOT retroactively mutate old groups.

The default path SHOULD create a successor group bound to the new immutable template version.

---

# ADM-050 - Report and Visualization Regression Verification

Report and visualization definitions SHOULD support regression verification using deterministic fixtures.

Regression checks SHOULD focus on semantic and accessibility correctness rather than relying only on pixel-perfect screenshots.

Potential assertions include:

- Every required metric is represented
- No unauthorized metric is represented
- Privacy-suppressed data is absent
- Missing values remain explicit
- Sort order is deterministic
- Labels map to the correct data
- Units are correct
- Axis scale is legal
- Required direct labels exist
- Accessible descriptions exist
- Tabular equivalents contain the same legal data
- Keyboard focus order is valid
- No required interaction is hover-only
- Layout remains usable at supported widths
- Text does not clip beyond declared tolerances
- High-contrast mode remains interpretable
- Grayscale/print remains interpretable when supported
- Color-blind simulation preserves distinctions
- Responsive projection preserves semantic ordering

Deterministic rendering snapshots MAY supplement these tests where useful, but screenshots MUST NOT be the only authority.

A changed report/visualization definition SHOULD show which regression assertions changed or failed.

---

# ADM-051 - Definition and State Diffing

Administrators SHOULD be able to inspect semantic diffs for important versioned artifacts.

Diff support SHOULD include:

- Group configuration versions
- Report definitions
- Dashboard definitions
- Visualization specifications
- Analysis expressions
- Privacy policies
- Policy packs
- Storage manifests
- Aggregate schema versions
- Configuration packages

Diffs SHOULD classify changes by meaning rather than only by serialized text.

Representative classifications include:

- PresentationOnly
- PrivacyRelevant
- ComparisonRelevant
- AggregateRelevant
- StorageRelevant
- AccessibilityRelevant
- Breaking
- NonBreaking
- RequiresRebuild
- RequiresRevalidation

The system SHOULD show the downstream impact of a diff using the change-impact dependency model.

---

# ADM-052 - Reversible Administration, Saved Views, and Capability Explanation

Administrator operations that conceptually support undo SHOULD implement undo as a new legal transition rather than mutation of historical state.

Examples include:

- Revert current dashboard definition to prior version
- Restore prior report definition as a new current version
- Reactivate a prior configuration where legal
- Reverse a non-destructive archive transition
- Restore a saved analysis definition

Undo MUST NOT:

- Erase immutable history
- Remove an accepted result that policy says is immutable
- Rewrite an old report snapshot
- Bypass storage/audit policy

Interactive analysis views SHOULD be bookmarkable when safe.

A bookmarkable analysis view MAY encode:

- Filters
- Sort
- Selected metrics
- Comparison references
- Visualization choice
- Drill-down location
- Dashboard/view identity

It MUST NOT encode credentials or hidden/suppressed data.

The application SHOULD provide a "Why can't I do this?" explanation for unavailable administrator actions.

Capability explanation SHOULD identify:

- Current state
- Missing capability
- Blocking guard
- Outstanding obligation
- Required evidence
- Relevant provider limitation
- Stable reason code

A disabled button without an explanation SHOULD be avoided for significant administrator actions.

---

# ADM-053 - Operational Diagnostics and Localization Preview

The administrator application SHOULD provide an explicit operational diagnostics area.

Diagnostics SHOULD surface:

- Storage provider connectivity
- Provider capability snapshot
- Authentication capability status without exposing secrets
- Read/write status
- Rate-limit state
- Manifest health
- Aggregate/index freshness
- Pending reconciliation obligations
- Schema compatibility
- Migration state
- Backup/restore readiness
- Last successful verified refresh evidence
- Degraded-mode status

Diagnostics MUST distinguish:

- Domain invalidity
- Provider unavailability
- Authentication failure
- Rate limiting
- Stale local state
- Corruption
- Unsupported capability
- Unknown external outcome

The report/dashboard builder SHOULD support localization preview.

Localization preview SHOULD detect:

- Text expansion/clipping
- Missing translations
- Unsupported glyphs where detectable
- Directionality issues
- Label collisions
- Table overflow
- Chart annotation overflow

Localization changes MUST NOT alter canonical data semantics.

---

# ADM-054 - End-to-End Derived-State Invalidation and Dependency Graph

Echelon Signal SHOULD model the administrator system as an explicit dependency graph extending from accepted submissions through final presentation artifacts.

Conceptually:

```
AcceptedSubmission
    -> SurveyResult
    -> AcceptedContributionSet
    -> SurveyGroupResult aggregate nodes
    -> Analysis measures
    -> Privacy/suppression decisions
    -> ReportData blocks
    -> VisualizationSpec nodes
    -> Dashboard/report projections
    -> Snapshot eligibility
```

Dependencies MUST be explicit or mechanically derivable.

When one authoritative input changes, the system SHOULD invalidate only the affected downstream closure.

Examples:

- A new accepted submission invalidates affected aggregate nodes, dependent analysis measures, dependent privacy decisions, dependent report blocks, and dependent visualization projections.
- A privacy-policy change invalidates privacy projections and downstream report/visualization output without requiring survey re-scoring.
- A report-definition presentation-only change invalidates report/visualization output without invalidating SurveyGroupResult.
- A template semantic change for a successor group does not invalidate historical immutable groups but may invalidate comparison eligibility.

Every derived node SHOULD be able to identify:

- Its input dependencies
- Its version
- Its current/stale state
- Its source hash/set
- Its rebuild/reprojection capability
- Its downstream dependents

The dependency graph SHOULD power:

- Incremental recomputation
- Change-impact preview
- Staleness detection
- Repair planning
- Capability explanation
- Lineage explanation
- Performance optimization
- Regression test targeting

A derived artifact MUST NOT be treated as current when any required upstream dependency has changed without a successful compatible reprojection.

The architecture SHOULD prove:

```
IncrementallyUpdatedDerivedClosure
=
FullReferenceRebuild
```

for every supported administrator transition where incremental execution is used.

This requirement intentionally pushes Ordo, ROS, Limen, evidence lineage, negative knowledge, unknown effects, and dependency-driven engineering across the complete administrator stack rather than only the respondent/scoring path.

---



# ADM-055 - Template Registry and Exact Template Resolution

The administrator application MUST have an explicit template-resolution subsystem rather than treating exact-template lookup as an incidental import operation.

The subsystem SHOULD support:

- One or more configured template registries
- Exact lookup by SurveyIdentifier, TemplateVersion, TemplateHash, or other canonical reference already permitted by the survey contract
- Local verified template cache
- Cache freshness and provenance
- Registry capability discovery
- Registry priority/order when multiple sources exist
- Explicit pinning to an exact immutable artifact
- Offline use of a previously verified exact template
- Recovery of a missing template from an alternate trusted registry
- Detection of conflicting artifacts claiming the same logical identity

A cached template MUST remain bound to its verified canonical hash.

A registry response that claims the correct version but produces the wrong hash MUST fail resolution.

The resolver MUST distinguish:

- ResolvedExact
- NotFoundInRegistry
- RegistryUnavailable
- RegistryUnauthorized
- HashMismatch
- ConflictingArtifacts
- UnsupportedTemplateVersion
- CachedVerifiedCopyAvailable
- ResolutionUnknown

Negative knowledge MUST remain scoped to the registry/revision actually searched.

The application MUST NOT silently replace an exact required template with "latest" or a merely compatible version.

Template registry credentials MUST follow the same secret-handling rules as storage-provider credentials.

---

# ADM-056 - Credential Lifecycle and Capability Revalidation

External-provider credentials MUST have an explicit lifecycle independent of domain data.

The administrator application MUST handle:

- Credential acquisition
- Validation
- Expiration
- Revocation
- Rotation
- Replacement
- Permission reduction
- Permission expansion
- Provider/account switching
- Logout/disconnection
- Revalidation after provider errors

Credential material MUST NOT be serialized into durable Signal domain state.

The application SHOULD derive a credential capability snapshot containing only non-secret capability evidence needed for state decisions.

A previously valid credential becoming invalid or less privileged MUST cause relevant write/read capabilities to be recomputed.

Credential failure MUST NOT mutate canonical survey/group/report data.

Credential replacement MUST NOT silently change the storage dataset being addressed.

Switching to a credential that resolves the same configured locator to a different repository/provider identity MUST require explicit administrator resolution.

The UI SHOULD distinguish:

- CredentialMissing
- CredentialExpired
- CredentialRejected
- PermissionInsufficient
- ProviderIdentityMismatch
- CredentialValidReadOnly
- CredentialValidReadWrite

---

# ADM-057 - Multiple Storage Profiles

The administrator application SHOULD support multiple storage profiles.

A storage profile SHOULD bind:

- StorageProvider type/version
- Provider locator
- Dataset/root namespace
- Branch or equivalent provider routing
- Non-secret capability/configuration settings
- Local display label
- Credential reference/slot
- Verification state

A profile display label MUST NOT be treated as authoritative domain identity and SHOULD remain local presentation metadata where practical.

Groups and datasets SHOULD reference a stable StorageProfileId or resolved provider locator contract rather than embedding credentials.

Changing a group's storage destination after authoritative data exists MUST use the storage-migration workflow.

The UI MUST NOT implement migration by simply editing a repository/path field on an existing authoritative dataset.

Profile deletion MUST be blocked while active datasets still depend on it unless those dependencies are migrated, archived under an explicit policy, or otherwise resolved.

---

# ADM-058 - GitHub Repository Growth, Rollover, and Compaction Strategy

The GitHub provider MUST account for long-term repository growth.

Signal SHOULD track or estimate:

- Current object count
- Current logical Signal dataset size
- Derived/index object size
- Snapshot growth
- Immutable-result growth
- Commit/write frequency attributable to Signal
- Partition depth
- Repository health thresholds

The provider architecture SHOULD support:

- Deterministic sharding
- Archive repositories
- Dataset rollover to a successor repository
- Compaction or replacement of rebuildable derived projections
- Removal of obsolete rebuildable caches from the active tree
- Provider migration when GitHub is no longer operationally appropriate

Compaction MUST NOT rewrite or discard immutable authoritative results merely to reduce size unless an explicit retention/deletion policy permits it.

Compacting rebuildable indexes/projections MUST preserve the authoritative hash set from which they can be reconstructed.

A repository rollover MUST use migration semantics and explicit destination activation.

Signal SHOULD provide warnings before repository scale is likely to make ordinary GitHub-backed operations unreliable or impractical.

---

# ADM-059 - Storage Cost and Operation Budget Estimation

Before large administrator operations, Signal SHOULD be able to estimate operational impact.

Estimates MAY include:

- Expected immutable objects
- Expected mutable manifest updates
- Expected provider reads
- Expected provider writes
- Expected bytes written
- Expected repository growth
- Expected index/materialized-view growth
- Expected snapshot growth
- Estimated browser memory demand
- Estimated full-rebuild work

The estimator MUST distinguish observed measurements from projections/estimates.

A storage or operation estimate MUST NOT be represented as a guaranteed billing cost unless the provider exposes a reliable pricing contract and Signal explicitly supports it.

The system SHOULD warn when a planned operation exceeds configured or evidence-based operational budgets.

A warning MAY recommend:

- Smaller batches
- Partitioning
- Deferred detail loading
- Archive/rollover
- Provider migration
- Narrower analysis
- Fewer retained snapshots

Operational warnings SHOULD create explicit advisory evidence but SHOULD NOT silently alter canonical behavior.

---

# ADM-060 - Batch Import Transaction and Resume Semantics

Batch import MUST define explicit transaction semantics.

Default recommended behavior:

- Each submission is validated and accepted/rejected independently.
- One invalid artifact does not roll back unrelated valid accepted submissions.
- The batch has a stable BatchImportId.
- Batch progress is resumable.
- Replaying the same batch is idempotent with respect to already accepted contributions.

A batch result SHOULD contain:

- BatchImportId
- InputArtifactCount
- ProcessedCount
- AcceptedCount
- RejectedCount
- DuplicateCount
- ReconciliationRequiredCount
- RemainingCount
- Stable per-item outcome references
- BatchStatus

Potential BatchStatus values:

- Pending
- Running
- Partial
- Complete
- CompleteWithRejections
- ReconciliationRequired
- CancelledBeforeProcessingRemainder

Cancellation MUST NOT roll back already committed independent accepted contributions unless a future explicit transactional provider mode is separately designed.

Batch order MUST NOT alter the final accepted contribution set.

A resumed batch MUST not double-count items processed before interruption.

---

# ADM-061 - Quarantine Boundary for Untrusted Artifacts

Signal SHOULD provide an explicit Quarantined state for untrusted imported artifacts before they become authoritative.

Artifacts that MAY enter quarantine include:

- Respondent submissions
- Batch import files
- Configuration packages
- Backup bundles
- Restored provider objects
- Migrated objects
- Manually modified repository content
- Future service import payloads

Quarantine state SHOULD permit:

- Structural validation
- Schema validation
- Integrity/hash verification
- Template resolution
- Privacy validation
- Resource-limit validation
- Reference validation
- Compatibility analysis

A quarantined artifact MUST NOT:

- Contribute to a SurveyGroupResult
- Appear in formal reports
- Change completion counts
- Become a report snapshot dependency
- Become active configuration
- Replace authoritative store objects

Promotion from Quarantined to an authoritative state MUST be an explicit legal transition with evidence that all required validations succeeded.

Rejected quarantine artifacts MAY retain only the non-PII evidence needed for diagnosis and audit under configured retention policy.

---

# ADM-062 - Conflict Resolution Workspace

The administrator application SHOULD provide an explicit conflict-resolution workflow for concurrent incompatible changes.

For a conflict the UI SHOULD be able to show:

- Base version
- Current authoritative version
- Proposed version
- Semantic diff
- Automatically mergeable independent changes
- Non-mergeable conflicting changes
- Downstream impact preview
- Required obligations after each resolution option

The system MAY automatically merge only changes proven independent under deterministic merge rules.

Examples of potentially mergeable changes:

- Two distinct immutable accepted contributions
- Independent dashboard block additions with deterministic ordering rules
- Independent non-overlapping configuration package additions where semantics permit

Examples requiring explicit resolution:

- Conflicting group configuration changes
- Different privacy thresholds
- Different storage destination changes
- Conflicting report-definition edits to the same block
- Competing policy-pack versions

A conflict resolution MUST create a new authoritative version/transition and MUST NOT rewrite historical versions.

---

# ADM-063 - Explicit Dependency Pinning for Reports, Dashboards, and Snapshots

Every formal reusable administrator definition SHOULD explicitly pin external semantic dependencies.

Dependencies MAY include:

- Survey/template semantic version/hash
- ReportDefinitionVersion
- DashboardDefinitionVersion
- VisualizationSpecVersion
- Visualization grammar version
- AnalysisExpressionLanguageVersion
- PolicyPackId/version
- Localization package/version
- Comparison dataset/reference/version
- Aggregate schema version
- Result schema version

"Latest" MUST NOT be used for a dependency whose later change could alter formal report meaning.

A draft authoring experience MAY offer "latest" as a convenience selector, but publication/formal snapshot creation MUST resolve it to an exact immutable dependency.

Missing pinned dependencies MUST produce an explicit unresolved state rather than silently substituting another version.

---

# ADM-064 - Reproducibility Versus Deletion Policy Conflict

Signal MUST define behavior when reproducibility requirements conflict with deletion/retention policy.

A policy decision MAY require removal of authoritative individual results while allowing a previously issued aggregate snapshot to remain.

The system MUST explicitly represent when:

- Full reconstruction is still possible
- Only aggregate/report reproduction is possible
- Underlying contribution-level reconstruction is no longer possible
- A formal snapshot remains valid as an immutable historical artifact but its original source set has been intentionally retired
- A deletion request or policy prevents future lineage expansion

Lineage MUST NOT falsely claim reconstructability after required source artifacts have been deleted.

Policy evaluation SHOULD identify whether deletion would break:

- Group aggregate rebuild
- Report snapshot rebuild
- Audit verification
- Comparison verification
- Migration verification

When policy requires deletion despite loss of reproducibility, the resulting limitation MUST be recorded as durable non-PII evidence.

---

# ADM-065 - Dataset Sealing

A group/dataset SHOULD support an optional Sealed state after formal completion.

A sealed dataset MUST NOT accept:

- New accepted contributions
- Configuration mutation
- Privacy-policy mutation that would redefine previously issued report meaning
- Storage-locator mutation outside migration
- Replacement of immutable accepted results

Allowed operations MAY include:

- Read
- Reproduce report snapshots
- Export under current policy
- Verify hashes/lineage
- Migrate storage without changing semantics
- Archive under policy
- Create derived comparisons that do not mutate the sealed dataset

Reopening a sealed dataset, if supported at all, MUST be an explicit high-consequence transition producing a successor state/version and clear audit evidence.

The default SHOULD favor creating a successor group rather than mutating a sealed one.

---

# ADM-066 - Group Close and Formal Finalization Ceremony

Group completion and formal administrator finalization MUST be distinct concepts.

A group MAY satisfy completion rules but still have unresolved operational obligations.

Before formal finalization/sealing, Signal SHOULD verify:

- Group completion policy satisfied or explicitly closed incomplete
- No unresolved import reconciliation
- No stale aggregate/index required for final report
- Privacy policy valid
- Required report dependencies resolvable
- Storage manifest healthy
- Required hashes valid
- Required formal report definition available
- Required provider write complete or reconciled
- Required acceptance evidence present

Potential finalization outcomes include:

- Finalized
- FinalizedIncomplete
- BlockedByObligations
- FinalizationOutcomeUnknown

Finalization MAY create a formal report snapshot as part of the same orchestrated workflow, but the snapshot remains a distinct immutable artifact.

A finalization operation with OutcomeUnknown MUST be reconciled before the group is treated as formally finalized.

---

# ADM-067 - Import Provenance Without Person Identity

Signal SHOULD record how an artifact entered the administrator system without recording who performed the action.

Representative ImportOrigin values:

- PastedUrl
- MultiPaste
- ImportedTextFile
- ImportedCanonicalArtifact
- RestoredBackup
- ProviderMigration
- ConfigurationPackage
- FutureServiceImport
- SyntheticSandbox

Import provenance MAY record:

- ImportOrigin
- BatchImportId
- Source artifact hash
- Provider/source type
- Migration/restore identifier
- Schema/encoding version
- Non-PII reason/evidence codes

Import provenance MUST NOT record administrator names, email addresses, GitHub usernames, IP addresses, or other person identifiers.

Provenance is operational lineage, not authentication identity.

---

# ADM-068 - Clock, Calendar, Time Zone, and Period Semantics

Any administrator feature depending on time MUST use explicit clock/calendar semantics.

Relevant features include:

- Historical trends
- Survey waves
- Report periods
- Snapshot display time
- Retention schedules
- Archive schedules
- Migration evidence
- Backup evidence
- Operational diagnostics

Canonical temporal evidence SHOULD use UTC instants where an instant is required.

Human-facing period definitions SHOULD explicitly identify:

- Time zone
- Calendar system where relevant
- Period start
- Period end
- Inclusivity/exclusivity rules
- Locale only for display formatting

Browser local time MUST NOT silently become authoritative domain time.

A report period such as "September" MUST resolve to explicit boundaries before formal publication when time zone could change membership.

Clock evidence MUST remain separate from deterministic result meaning unless the requirement intentionally includes time as an input.

---

# ADM-069 - Internationalization and Bidirectional Layout Semantics

Administrator/report internationalization MUST extend beyond translated strings.

The system SHOULD support or explicitly validate:

- Right-to-left layout
- Bidirectional text
- Locale-aware number formatting
- Locale-aware percentages
- Decimal/group separators
- Locale-aware date/time display
- Pluralization
- Long translated labels
- Non-Latin scripts
- Font fallback
- Table direction
- Chart label direction
- Mirrored layout where appropriate

Canonical numeric/date/result data MUST remain locale independent.

Localization MUST be a presentation transformation over canonical data.

A locale MUST NOT change:

- Sorting semantics unless the definition explicitly requests locale-aware label sorting
- Scoring
- Aggregation
- Privacy thresholds
- Comparison eligibility
- Hashes of canonical result data

Localization regression fixtures SHOULD include at least one right-to-left locale and one locale with materially different number/date formatting.

---

# ADM-070 - Offline and Interrupted Administrator Session Behavior

The administrator application MUST define behavior when network connectivity is lost or provider access is interrupted.

When verified immutable data is already loaded, the application MAY continue to allow local read-only operations such as:

- Viewing verified reports
- Viewing cached snapshots
- Running deterministic local analysis over already authorized loaded data
- Inspecting lineage already present locally

Mutating capabilities MUST become unavailable unless a future explicit offline-operation journal is designed.

The application MUST NOT pretend a write succeeded while offline.

A pending provider request interrupted before outcome is known MUST use OutcomeUnknown semantics where applicable.

On reconnection, the application MUST revalidate provider version/change evidence before permitting stale mutations.

An offline UI MUST clearly indicate:

- Offline/degraded state
- Last verified refresh evidence
- Whether displayed data may be stale
- Which actions are unavailable

---

# ADM-071 - Browser Secret Storage Policy

Signal MUST define how provider credentials may be retained in the browser.

Supported modes SHOULD be explicit, for example:

- MemoryOnly
- SessionScoped
- PersistentBrowserStorage only by explicit administrator choice where supported

The safest practical mode SHOULD be the default.

Credential material MUST NOT enter:

- URL/query/fragment state
- Browser history
- report/dashboard definitions
- AnalysisState
- exports
- repository content
- telemetry
- crash logs

If persistent browser storage is supported, the UI MUST clearly disclose the persistence behavior and provide a way to clear the credential.

A browser secret policy MUST distinguish credential retention from ordinary non-secret administrator preferences.

---

# ADM-072 - Cross-Tab Credential and Capability Coherence

Multiple administrator tabs may share provider/session state while holding different domain revisions.

Signal SHOULD define cross-tab behavior for:

- Credential rotation
- Credential removal/logout
- Permission reduction
- Storage-profile changes
- Dataset sealing
- Group finalization
- Provider migration activation

Where the browser permits reliable signaling, a tab observing a credential/session invalidation SHOULD downgrade affected capabilities promptly.

A stale tab MUST still rely on provider version/concurrency checks even if cross-tab notifications exist.

Cross-tab notification is an optimization, not the authority for conflict prevention.

No tab may assume its credential capability snapshot remains valid indefinitely without provider evidence.

---

# ADM-073 - GitHub Branch Protection and Pull Request Storage Mode

The GitHub provider MUST detect when the configured branch does not permit direct writes.

The provider SHOULD distinguish:

- DirectWriteAvailable
- ReadOnlyByPermission
- ProtectedBranchRequiresReview
- BranchMissing
- RepositoryArchived
- ProviderPolicyUnknown

Phase 1 MAY support only direct-write repositories, but unsupported protected-branch behavior MUST fail explicitly.

A future GitHub PullRequestWrite capability MAY be added.

If implemented, PR-based storage writes MUST remain provider mechanics and MUST NOT change Signal domain semantics.

A PR branch MUST NOT become authoritative until the provider contract can establish that the intended canonical objects are active in the configured authoritative branch.

Pending PR state MUST not be reported as a completed durable Signal write.

---

# ADM-074 - Operational Repair Preview and Plan

Before a repair operation mutates durable state, Signal SHOULD produce a repair plan.

Repairable operations include:

- Rebuild aggregate
- Rebuild index/materialized view
- Repair manifest references
- Restore backup
- Resume migration
- Reconcile orphan immutable objects
- Reproject AdminReportState
- Rebuild comparison indexes

The repair plan SHOULD identify:

- Objects to read
- Objects to create
- Mutable references to change
- Immutable authoritative objects guaranteed not to change
- Expected source hash/set
- Expected resulting hash
- Provider operations required
- Estimated operation budget
- Rollback/recovery strategy where available
- Validation required after repair

Repair execution MUST verify that the source state still matches the plan's preconditions.

If upstream state changed after plan creation, the repair MUST stop or re-plan rather than applying a stale plan blindly.

---

# ADM-075 - Invariant Health Dashboard

The administrator system SHOULD expose an invariant health view showing whether the dataset is internally coherent.

Potential health checks include:

- Storage manifest valid
- Dataset identity consistent
- Accepted contribution index matches immutable objects
- Current aggregate source hash matches accepted set
- Current AdminReportState matches current SurveyGroupResult
- Materialized indexes current
- No unresolved OutcomeUnknown obligations
- No unresolved provider conflicts
- Pinned report dependencies resolvable
- Formal snapshots reproducible to their declared level
- Privacy policy valid
- No active suppression-policy violation
- Storage provider capability still sufficient
- Migration state consistent
- Backup verification status
- Sealed/finalized state consistent
- Schema versions supported

Each invariant SHOULD report:

- Healthy
- Warning
- Unhealthy
- Unknown
- NotApplicable

The health view MUST distinguish an unverified check from a passing check.

A health summary SHOULD link directly to the obligation/repair/action needed to resolve a failed invariant.

---

# ADM-076 - Proof-Carrying Derived Artifacts

Derived administrator artifacts SHOULD carry compact derivation evidence sufficient to verify their relationship to authoritative inputs without trusting their storage location.

Candidate artifacts include:

- SurveyGroupResult
- AdminReportState
- Materialized aggregate/index
- Analysis result
- ReportData
- VisualizationSpec projection
- Report snapshot

A derivation certificate MAY contain fields conceptually equivalent to:

```
DerivationCertificate
    CertificateVersion
    ArtifactType
    InputSetHash
    ConfigurationHash
    EvaluatorVersion
    DependencyClosureHash
    OutputHash
    SchemaVersions
    PolicyHash?
```

The exact certificate MUST remain deterministic and free of PII and credentials.

A certificate is evidence of derivation consistency, not a cryptographic signature of a person.

The system SHOULD be able to validate:

```
Stored artifact
+
DerivationCertificate
+
Available authoritative inputs/configuration
->
Verified | Invalid | InputsUnavailable | UnsupportedCertificateVersion
```

Proof-carrying artifacts SHOULD support:

- Cache validation
- Migration verification
- Backup verification
- Repair verification
- Report snapshot verification
- Stale-state detection
- Cross-provider equivalence

If a certificate cannot be verified because required inputs were intentionally deleted under policy, the result MUST be InputsUnavailable or equivalent, not Verified.

This capability SHOULD extend existing lineage rather than create a competing provenance system.

---


# ADM-077 - Required Aegis Integration and Operational Fault Boundaries

Echelon Signal MUST import and actively use Aegis as the standard mechanism for unexpected operational failure.

The application MUST reference:

- `EchelonFoundry.Aegis.Core`

Because the phase-1 administrator storage provider calls the GitHub API directly, the applicable GitHub integration MUST also be referenced and used:

- `EchelonFoundry.Aegis.Integration.GitHub`

If Signal persists Aegis fault/lifecycle events in GitHub, it MUST use the standard GitHub store adapter rather than inventing a Signal-specific Aegis event format:

- `EchelonFoundry.Aegis.Store.GitHub`

The exact package versions MUST be pinned according to the repository's dependency/versioning policy.

Merely adding package references does not satisfy this requirement.

Signal MUST configure Aegis once during application/bootstrap initialization and validate that configuration before relying on it.

Aegis MUST be used at architectural boundaries where unexpected operational failure can originate outside the authoritative Signal domain model, including as applicable:

- GitHub API/network access
- GitHub storage-provider reads and writes
- Template registry access
- Browser/file import mechanism boundaries
- Backup/restore provider I/O
- Storage migration provider I/O
- Export/download mechanism boundaries where failures originate outside domain logic
- Future HTTP/service storage providers
- Other filesystem, network, browser interop, or external-service boundaries introduced later

Signal SHOULD maintain an explicit boundary declaration compatible with Aegis guidance, such as an `aegis-boundaries.json` artifact or the then-current Aegis boundary declaration mechanism.

Aegis MUST NOT replace Signal/Ordo domain outcomes.

The following remain typed domain/state outcomes rather than Aegis faults:

- Illegal state transition
- Duplicate accepted submission
- Privacy suppression
- NotComparable
- Unsupported provider capability
- Quarantined artifact
- Validation failure
- Missing required evidence
- Dataset sealed
- Group not complete
- Other expected outcomes already represented by Signal discriminated unions, capabilities, obligations, or transition results

Programming defects MUST continue to fail loudly according to Aegis policy rather than being swallowed or converted into ordinary domain failures.

Bare catch-all handling that swallows unexpected operational failure is prohibited.

An intercepted operational failure MUST terminate in an explicit path such as:

- Propagation
- Verified recovery
- A proposed Ordo/SDE state transition
- A declared suppression policy that emits the applicable Aegis lifecycle evidence

Aegis MAY propose recovery/state transitions, but Ordo/SDE remains the authority deciding whether those transitions are legal.

Retry through Aegis recovery MUST occur only when the operation is known to be safe/idempotent.

A write with an uncertain remote outcome MUST preserve Signal's `OutcomeUnknown` / reconciliation semantics and MUST NOT be converted into a generic retry merely because Aegis captured the fault.

Signal SHOULD use stable application-specific Aegis fault codes for its boundaries.

GitHub failures SHOULD use the standard Aegis GitHub failure translation model where applicable rather than independently classifying raw GitHub exceptions at every call site.

Aegis fault context MUST respect Signal's no-PII and credential rules.

Fault context, diagnostic events, sinks, and exported diagnostics MUST NOT contain:

- GitHub tokens
- Other provider credentials
- PII
- Raw respondent URLs when they could contain answer state
- Unredacted sensitive configuration
- Hidden/suppressed report data

Context values MUST use the applicable Aegis privacy classification/redaction mechanisms.

Limen SHOULD render user-facing fault presentation from Aegis presentation intent/structure rather than inventing an independent browser fault taxonomy.

Aegis remains the operational-fault authority; Limen remains the browser rendering mechanism; Ordo remains the legal-state-transition authority.

Testing MUST prove actual Aegis integration.

At minimum:

- Tests use Aegis replaceable/collector sinks rather than asserting console output.
- Each integration boundary has deterministic translation/classification tests.
- GitHub authentication, authorization, not-found, rate-limit, timeout/network, malformed-response, and uncertain-write scenarios are covered where the integration exposes them.
- Fault context redaction is tested for tokens, PII-like values, and respondent URL leakage.
- Programming defects are verified not to be silently captured as recoverable operational faults.
- Domain outcomes are verified not to be routed through Aegis.
- Recovery authorization remains controlled by Ordo/SDE policy.
- Unknown-effect writes remain reconciliation obligations.
- Repeated/transient faults do not produce uncontrolled UI notification storms.
- Aegis degraded/sink-failure behavior does not corrupt or block authoritative Signal domain state unless a configured required-sink policy explicitly requires the operation to stop.

Acceptance evidence MUST demonstrate both:

```
Aegis package/reference present
AND
Aegis exercised at declared operational boundaries
```

A build that references Aegis but bypasses it with ad hoc `try/with`, raw exception handling, or a parallel Signal-specific operational fault framework does not satisfy this requirement.

---

# Cross-Cutting Administrator Invariants

1. Echelon Signal administrator state contains no PII.
2. Storage choice does not change survey or report meaning.
3. GitHub is a provider implementation, not the domain model.
4. Every accepted contribution is immutable and idempotent.
5. Independent accepted contributions aggregate deterministically regardless of import order.
6. Rejected imports never affect aggregates.
7. Unknown external effects are reconciled before unsafe retry.
8. Mutable manifests use version/compare-and-swap semantics.
9. Derived indexes and aggregates are rebuildable from authoritative immutable inputs.
10. Incremental aggregation equals full recomputation.
11. Report rendering never re-scores surveys.
12. Analysis expressions never mutate canonical results.
13. Comparisons require explicit semantic compatibility.
14. Missing or suppressed values are never silently rendered as zero.
15. Privacy suppression occurs before data reaches visualization components.
16. Interactive filtering cannot bypass anonymous minimum-count rules.
17. Chart semantics are declarative and validated.
18. Every chart has an accessible non-visual/tabular equivalent.
19. Color is never the only carrier of meaning.
20. Report definitions and dashboards are versioned.
21. Formal report snapshots are immutable and reproducible.
22. Credentials never enter URLs, report data, exports, or repository content.
23. Provider failure cannot silently turn a write-capable state into a successful completed state.
24. Degraded/read-only state is explicit.
25. Storage migration is verified before destination activation.
26. Schema migration does not silently change canonical scores.
27. Lineage can trace formal report values to authoritative aggregate/result inputs without exposing PII.
28. Large datasets do not require loading every immutable result for ordinary overview/report use.
29. Rate limits and resource limits are explicit external/domain evidence, not generic exceptions.
30. Browser/TypeScript code renders mechanisms; F# remains the authority for application meaning.
31. Exact immutable template resolution never substitutes latest or merely compatible artifacts.
32. Credential lifecycle changes capabilities but never canonical domain state.
33. Storage destination changes for authoritative datasets use migration semantics.
34. Batch import commits independent valid contributions idempotently and resumably.
35. Quarantined artifacts cannot affect authoritative results before explicit validated promotion.
36. Formal dependencies are pinned to exact versions before snapshot/publication.
37. Deletion policy never leaves lineage claiming reproducibility that no longer exists.
38. Sealed datasets reject ordinary mutation and new contributions.
39. Formal finalization verifies unresolved obligations before declaring final state.
40. Temporal semantics use explicit clock/time-zone/period evidence rather than implicit browser local time.
41. Localization and bidirectional layout never change canonical result meaning.
42. Offline/interrupted sessions never imply successful writes without provider evidence.
43. Credentials never persist beyond the configured browser secret-storage policy.
44. Branch protection and PR workflow are provider mechanics, never silent durable-write success.
45. Repair execution verifies preconditions against the state used to create the repair plan.
46. Invariant checks distinguish unknown/unverified from healthy.
47. Derived artifact certificates extend lineage and can be independently validated when required inputs remain available.
48. Aegis is required and must be exercised at operational boundaries; package presence alone is insufficient.
49. Aegis faults never replace typed Signal/Ordo domain outcomes.
50. Aegis recovery may propose but never authorize an Ordo transition or unsafe retry.
51. Aegis diagnostics obey the same no-PII, no-secret, and respondent-data redaction boundaries as the rest of Signal.

---

# Requirements Migration Status

Requirements-definition status: COMPLETED  
ROS work-item reconciliation completed: 2026-09-22

These administrator requirements are authoritative input requirements. Every actionable statement in ADM-001 through ADM-077 is assigned to at least one bounded ROS delivery item.

The administrator extension is decomposed into:

| Work item | Delivery scope |
|---|---|
| WI-0011 | Administrator application state, group management, and Limen UX |
| WI-0012 | Storage provider contract and GitHub repository provider |
| WI-0013 | Administrator import, concurrency, durable aggregation, and indexes |
| WI-0014 | Administrator analytics, comparisons, lineage, and dependency invalidation |
| WI-0015 | Typed visualization grammar, dashboards, and accessibility |
| WI-0016 | Report builder, snapshots, exports, configuration packages, and policy packs |
| WI-0017 | Administrator privacy, security, audit, retention, and deletion lifecycle |
| WI-0018 | Storage migration, backup, schema evolution, and operational recovery |
| WI-0019 | Administrator sandbox, synthetic data, advanced analytics extensions, and phase boundary |
| WI-0020 | Administrator cross-cutting verification, performance, and acceptance program |

The older WI-0008, WI-0009, and WI-0010 items remain the pre-existing administrator/reporting/cross-cutting baseline. WI-0011 through WI-0020 decompose this newer administrator extension and depend on those baseline contracts where noted. They do not replace or narrow the earlier authoritative requirements.

## ADM-to-ROS Mapping

| Requirement group | Section | ROS work item(s) |
|---|---|---|
| ADM-001 | Administrator Product Boundary and No-PII Contract | WI-0011 |
| ADM-002 | Administrator State System | WI-0011 |
| ADM-003 | Storage Provider Abstraction | WI-0012 |
| ADM-004 | Phase 1 GitHub Repository Storage Provider | WI-0012 |
| ADM-005 | Canonical Storage Manifest | WI-0012 |
| ADM-006 | Future Installable Service Storage Option | WI-0012, WI-0018 |
| ADM-007 | Group Administration | WI-0011 |
| ADM-008 | Submission Intake and Import Queue | WI-0013 |
| ADM-009 | Import Idempotency, Concurrency, and Unknown Effects | WI-0013 |
| ADM-010 | Durable Result and Aggregate Storage Model | WI-0013 |
| ADM-011 | Incremental Aggregation Engine | WI-0013 |
| ADM-012 | Query and Analysis Model | WI-0014 |
| ADM-013 | Statistical and Measurement Analysis | WI-0014 |
| ADM-014 | Cross-Group, Historical, and Version Comparison | WI-0014 |
| ADM-015 | Signal Visualization Grammar | WI-0015 |
| ADM-016 | Visualization Suitability and Anti-Misleading Rules | WI-0015 |
| ADM-017 | Visualization Accessibility | WI-0015 |
| ADM-018 | Dashboard System | WI-0015 |
| ADM-019 | Interactive Exploration and Drill-Down | WI-0014 |
| ADM-020 | Data-Lineage Explorer | WI-0014, WI-0016 |
| ADM-021 | Report Builder | WI-0016 |
| ADM-022 | Report Snapshots and Reproducibility | WI-0016 |
| ADM-023 | Exports | WI-0016 |
| ADM-024 | Privacy-Preserving Aggregation and Disclosure Controls | WI-0017 |
| ADM-025 | Storage Security and Integrity | WI-0012, WI-0017 |
| ADM-026 | GitHub Rate Limits, Scale, and Performance | WI-0012 |
| ADM-027 | Rebuildable Indexes and Materialized Views | WI-0013 |
| ADM-028 | Storage Migration and Provider Portability | WI-0018 |
| ADM-029 | Backup, Restore, and Disaster Recovery | WI-0018 |
| ADM-030 | Audit Without PII | WI-0017 |
| ADM-031 | Administrator UX Information Architecture | WI-0011 |
| ADM-032 | Search, Filtering, and Saved Analysis | WI-0011 |
| ADM-033 | Read-Only and Degraded Modes | WI-0011, WI-0018 |
| ADM-034 | Schema Evolution and Compatibility | WI-0018 |
| ADM-035 | Limen Boundary for Administrator UI | WI-0011 |
| ADM-036 | Advanced Stress and Adversarial Test Program | WI-0013, WI-0020 |
| ADM-037 | Property and Model-Based Tests | WI-0013, WI-0020 |
| ADM-038 | Performance and Resource Limits | WI-0020 |
| ADM-039 | Optional Advanced Analytics Extensions | WI-0019 |
| ADM-040 | Phase Boundaries | WI-0019 |
| ADM-041 | Change-Impact Preview and Obligation Planning | WI-0014 |
| ADM-042 | Administrator Sandbox and Simulation Mode | WI-0019 |
| ADM-043 | Synthetic and Adversarial Survey Data Generator | WI-0019 |
| ADM-044 | Storage Provider Conformance Suite | WI-0012, WI-0020 |
| ADM-045 | Data Lifecycle, Retention, Archival, and Deletion Semantics | WI-0017, WI-0018 |
| ADM-046 | Durable Store as Untrusted Input | WI-0012 |
| ADM-047 | Portable Configuration Packages | WI-0016 |
| ADM-048 | Versioned Policy Packs | WI-0016, WI-0017 |
| ADM-049 | Template Upgrade and Successor-Group Impact Analysis | WI-0014 |
| ADM-050 | Report and Visualization Regression Verification | WI-0015, WI-0020 |
| ADM-051 | Definition and State Diffing | WI-0014 |
| ADM-052 | Reversible Administration, Saved Views, and Capability Explanation | WI-0011, WI-0017 |
| ADM-053 | Operational Diagnostics and Localization Preview | WI-0011, WI-0018 |
| ADM-054 | End-to-End Derived-State Invalidation and Dependency Graph | WI-0014, WI-0020 |
| ADM-055 | Template Registry and Exact Template Resolution | WI-0012, WI-0014 |
| ADM-056 | Credential Lifecycle and Capability Revalidation | WI-0011, WI-0012, WI-0017 |
| ADM-057 | Multiple Storage Profiles | WI-0011, WI-0012, WI-0018 |
| ADM-058 | GitHub Repository Growth, Rollover, and Compaction Strategy | WI-0012, WI-0018, WI-0020 |
| ADM-059 | Storage Cost and Operation Budget Estimation | WI-0012, WI-0020 |
| ADM-060 | Batch Import Transaction and Resume Semantics | WI-0013, WI-0020 |
| ADM-061 | Quarantine Boundary for Untrusted Artifacts | WI-0013, WI-0017, WI-0020 |
| ADM-062 | Conflict Resolution Workspace | WI-0011, WI-0014 |
| ADM-063 | Explicit Dependency Pinning for Reports, Dashboards, and Snapshots | WI-0014, WI-0016 |
| ADM-064 | Reproducibility Versus Deletion Policy Conflict | WI-0016, WI-0017, WI-0018 |
| ADM-065 | Dataset Sealing | WI-0011, WI-0017 |
| ADM-066 | Group Close and Formal Finalization Ceremony | WI-0011, WI-0016, WI-0017 |
| ADM-067 | Import Provenance Without Person Identity | WI-0013, WI-0017 |
| ADM-068 | Clock, Calendar, Time Zone, and Period Semantics | WI-0011, WI-0014, WI-0016, WI-0020 |
| ADM-069 | Internationalization and Bidirectional Layout Semantics | WI-0015, WI-0016, WI-0020 |
| ADM-070 | Offline and Interrupted Administrator Session Behavior | WI-0011, WI-0012, WI-0020 |
| ADM-071 | Browser Secret Storage Policy | WI-0011, WI-0017 |
| ADM-072 | Cross-Tab Credential and Capability Coherence | WI-0011, WI-0012, WI-0020 |
| ADM-073 | GitHub Branch Protection and Pull Request Storage Mode | WI-0012, WI-0020 |
| ADM-074 | Operational Repair Preview and Plan | WI-0014, WI-0018, WI-0020 |
| ADM-075 | Invariant Health Dashboard | WI-0011, WI-0014, WI-0018 |
| ADM-076 | Proof-Carrying Derived Artifacts | WI-0013, WI-0014, WI-0016, WI-0018, WI-0020 |
| ADM-077 | Required Aegis Integration and Operational Fault Boundaries | WI-0011, WI-0012, WI-0013, WI-0017, WI-0018, WI-0020 |

Complete migration ledger: `docs/requirements/survey-engine-requirements-migration.md`
